[providers]
python = "3.11"

[phases.setup]
nixPkgs = ["python311", "gobject-introspection", "pango", "harfbuzz", "fontconfig"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn src.api.app:app --host 0.0.0.0 --port $PORT & python run_bot.py"
